<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>All the Dojos</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css"
		/>
	</head>
	<body>
		<section class="section has-background-link-dark">
			<p class="title has-text-white has-text-centered">Dojos</p>
			<div class="container">
			<% if flash[:success] %>
				<div class="notification is-success">
					<p>
						<%= flash[:success] %>
					</p>
				</div>
			<% end %>
				<div class="box">
					<div class="content">
						<a class="is-size=5" href="/dojo/new">Add a Dojo</a>
						<p class="title">Listing <%= @dojo_count %> Dojos</p>
						<table
							class="table is-bordered is-hoverable is-fullwidth"
							style="text-align: center"
						>
							<thead>
								<tr>
									<th>Coding Dojo Branch</th>
									<th>Street</th>
									<th>City</th>
									<th>State</th>
									<th>Actions</th>
								</tr>
							</thead>
							<tbody>
								<% @dojos.each do |dojo| %>
								<tr>
									<td class="is-vcentered"><%= dojo.branch %></td>
									<td class="is-vcentered"><%= dojo.street %></td>
									<td class="is-vcentered"><%= dojo.city %></td>
									<td class="is-vcentered"><%= dojo.state %></td>
									<td class="is-vcentered"><a href="/dojo/<%= dojo.id %>">Show</a> | <a href="/dojo/<%= dojo.id %>/edit">Edit</a> | <a href="/dojo/<%= dojo.id %>" data-method="delete">Destroy</a></td>
								</tr>
								<% end %>
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</section>
	</body>
</html>
