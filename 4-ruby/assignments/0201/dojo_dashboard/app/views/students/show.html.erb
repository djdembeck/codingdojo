<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>All the Dojos</title>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css"
		/>
	</head>
	<body>
		<section class="section has-background-link-dark">
			<p class="title has-text-white has-text-centered">Dojos</p>
			<div class="container">
			<% if flash[:success] %>
				<div class="notification is-success">
					<p>
						<%= flash[:success] %>
					</p>
				</div>
			<% end %>
				<div class="box">
				<div class="content">
					<a class="is-size=5" href="/dojos">Home</a>
					<p class="title"><%= @dojo.city %>'s <%= @student.first_name %> <%= @student.last_name %></p>
				</div>
					<div class="content">
						<p>Email: <%= @student.email %></p>
												<p class="title">Student Roster</p>
						<table
							class="table is-bordered is-hoverable is-fullwidth"
							style="text-align: center"
						>
							<thead>
								<tr>
									<th>First Name</th>
									<th>Last Name</th>
									<th>Email</th>
									<th>Actions</th>
								</tr>
							</thead>
							<tbody>
								<% @students.each do |student| %>
									<% unless student.id == @student.id %>
									<tr>
										<td class="is-vcentered"><%= student.first_name %></td>
										<td class="is-vcentered"><%= student.last_name %></td>
										<td class="is-vcentered"><%= student.email %></td>
										<td class="is-vcentered"><a href="/dojos/<%= @dojo.id %>/students/<%= student.id %>">Show</a> | <a href="/dojos/<%= @dojo.id %>/students/<%= student.id %>/edit">Edit</a> | <a href="/dojos/<%= @dojo.id %>/students/<%= student.id %>" data-method="delete">Destroy</a></td>
									</tr>
									<% end %>
								<% end %>
							</tbody>
						</table>
						<a href="/dojos/<%= @student.id %>/edit" class="button is-warning">Edit</a>
					</div>
				</div>
			</div>
		</section>
	</body>
</html>
