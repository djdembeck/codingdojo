<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title><%= cat.name %> details</title>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
</head>
<body>
	<section class="section has-background-danger-light">
		<div class="container">
			<div class="box">
				<h1 class="title has-text-centered"><%= cat.name %></h1>
				<div class="container">
					<table class="table is-bordered is-fullwidth" style="text-align: center;;">
						<div class="box has-background-grey-lighter has-text-centered">
							<img src="/cat-<%= cat.id %>.jpg" alt="cat">
						</div>
						<thead>
							<tr>
								<th>Age</th>
								<th>Favorite food</th>
								<th>Favorite nap spots</th>
							</tr>
						</thead>
						<tbody>
							<tr>
								<td><%= cat.age %></td>
								<td><%= cat.food %></td>
								<td><% for (spot in cat.sleeping_spot) { %>
									<%= cat.sleeping_spot[spot] %><% if (spot != cat.sleeping_spot.length - 1) { %>, <% } %>
									<% } %></td>
							</tr>
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</section>
</body>
</html>